# Spotify / App configuration

# Your Spotify app client id (from developer dashboard)
SPOTIFY_CLIENT_ID=

# Optional for server-to-server (PKCE does not require it)
SPOTIFY_CLIENT_SECRET=

# Base URL where the app is served
# For local dev this should match vite dev server (package.json -> dev script)
APP_URL=http://localhost:3000

# Must be registered in the Spotify app dashboard
SPOTIFY_REDIRECT_URI=https://localhost:3000/auth/spotify/callback

# Space-delimited scopes requested during auth
SPOTIFY_SCOPES=user-read-email user-read-private user-read-playback-state user-modify-playback-state user-read-currently-playing playlist-read-private playlist-modify-private playlist-modify-public user-read-recently-played

# Session secret for signing cookies (generate a strong random string)
SESSION_SECRET=
